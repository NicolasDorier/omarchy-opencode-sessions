#!/usr/bin/env bash
set -euo pipefail

root="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)"

node --experimental-strip-types "$root/tests/state-hook.test.mjs"
"$root/tests/session-scan.test.sh"
shellcheck "$root/scripts/session-scan" "$root/scripts/install-opencode-hook" "$root/scripts/uninstall-opencode-hook" "$root/tests/session-scan.test.sh"
omarchy plugin validate "$root"
